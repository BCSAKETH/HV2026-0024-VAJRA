NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api

# Used ONLY for the Live Topology Map / Security Inbox's Supabase Realtime
# subscription (see supabase/migrations/0003_phase5_realtime.sql for why).
# The anon key is safe to ship to the browser by design — it can only ever
# read tracking_events rows that have a bag_id set, nothing else.
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
